# runningstone.github.io

Personal academic homepage for Shi Pan — https://runningstone.github.io

This repository holds **published output only**. GitHub Pages serves `docs/` (source =
`main` / `docs`). Do not hand-edit the HTML: it is rendered by a build script that lives
with the site's content in a separate, private workshop repository, and the whole `docs/`
tree is regenerated on every change.
